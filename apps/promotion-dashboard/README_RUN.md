# Run Promotion Dashboard

로컬 파일(index.html)을 바로 열면 브라우저 보안 정책 때문에 data.json 로드가 실패할 수 있습니다.
아래 스크립트로 서버를 띄워서 여세요:

```bash
cd /Users/min/.openclaw/workspaces/seogi
./scripts/run_promotion_dashboard.sh
```

열리는 주소:
- http://127.0.0.1:8787
